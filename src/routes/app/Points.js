import React from 'react'
import styled from 'styled-components/macro'

const Points = ({ value }) => {
  return (
    <div>
      <h2>Points</h2>
      <div>{value}</div>
    </div>
  )
}

export default Points
